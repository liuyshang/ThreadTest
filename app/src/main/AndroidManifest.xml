<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lance.threadtest">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/thread"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.PostActivity"
            android:label="post" />
        <activity
            android:name=".ui.MessageActivity"
            android:label="message" />
        <activity
            android:name=".ui.AsyncTaskActivity"
            android:label="asynctask" />
        <activity
            android:name=".ui.JavaThreadActivity"
            android:label="thread" />
        <activity
            android:name=".ui.ThreadPoolActivity"
            android:label="threadpool" />
    </application>

</manifest>
